class Solution:
    def invertTree(self, root: Optional[TreeNode]) -> Optional[TreeNode]:
        # if root is []:
        #     return root
        # # must be recursive right?
        # # base case ^ return root 
        # # do it for 1 i.e. ex2
        # #if root.left is not None:
        # temp = root.left.val
        # #if root.right is not None:
        # root.left.val = root.right.val
        # root.right.val = temp
        # return root
        